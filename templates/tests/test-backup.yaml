apiVersion: v1
kind: Pod
metadata:
  name: {{ include "postgresRclone.test.backup" . }}
  labels:
    {{- include "postgresRclone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  {{- include "postgresRclone.backup.spec" . | nindent 2 }}
