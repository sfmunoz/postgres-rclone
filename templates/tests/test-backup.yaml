apiVersion: v1
kind: Pod
metadata:
  name: {{ include "postgres.test.backup" . }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  {{- include "postgres.backup.spec" . | nindent 2 }}
