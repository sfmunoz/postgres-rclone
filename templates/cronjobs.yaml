apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "postgresRclone.fullname" . }}-backup
  labels:
    {{- include "postgresRclone.labels" . | nindent 4 }}
spec:
  schedule: "0 0 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          {{- include "postgresRclone.backup.spec" . | nindent 10 }}
